# Patterns Reference — Project Name

One entry per pattern actually used in this project. Only document what's real — don't pad with patterns you didn't use.

## <Pattern Name>

| Field           | Detail                                                                       |
| --------------- | ---------------------------------------------------------------------------- |
| What            | One-line definition                                                          |
| When to use     | Condition/signal for reaching for it                                         |
| When NOT to use | Common overuse case or a related pattern that fits better in some situations |
| Used in         | File/component in this project                                               |

```tsx
// minimal illustrative snippet from this project
```

**Tradeoff:** what you gain vs. what it costs.

---

<!--
Copy the block above for each additional pattern.
If two related patterns are easy to confuse (e.g. useDebounce vs useDeferredValue,
useDeferredValue vs useTransition), add a short comparison table instead of
duplicating full entries for each:

## Pattern A vs Pattern B — when each fits

| Pattern | Fits when |
|---|---|
| A | ... |
| B | ... |
-->
